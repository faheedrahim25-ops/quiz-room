# Mentor360 Database Quick Reference

Apply migrations in lexical order using a dedicated migration tool. The initial normalized PostgreSQL schema is `backend/migrations/001_initial.sql`; design decisions and constraints are documented in `DATABASE_SCHEMA.md`.
