# Mentor360 API Quick Reference

Base URL: `http://localhost:4000/api/v1` during development.

Responses use `success/data/message` for success and `success/error/code/message` for failures. The current foundation exposes `GET /health` and `GET /dashboard/mentor`; feature endpoint contracts are in `API_SPECIFICATION.md`.
